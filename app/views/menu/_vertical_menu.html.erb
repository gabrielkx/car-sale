<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Menu Vertical</title>
    <style>
        /* Estilos para o menu vertical */
        .sidebar {
            width: 250px; /* Largura do menu vertical */
            position: fixed; /* Fixar o menu à esquerda */
            top: 0;
            left: 0;
            height: 100%; /* Altura total da página */
            background-color: #333; /* Cor de fundo do menu */
            color: #fff; /* Cor do texto do menu */
            padding: 20px;
            z-index: 1; /* Adicione z-index para garantir que o menu fique na frente */
        }

        .sidebar a {
            display: block;
            color: #fff; /* Cor do link no menu */
            text-decoration: none;
            padding: 10px 0;
        }

        .sidebar a:hover {
            background-color: #555; /* Cor de fundo ao passar o mouse */
        }

        /* Estilos para o conteúdo principal */
        .content {
            margin-left: 270px; /* Largura do menu + uma margem para separação */
            margin-top: 80px; /* Altura do cabeçalho + margem */
            padding: 20px;
        }
    </style>
</head>
<body>
    <div class="sidebar">
        <h2>Menu</h2>
        <ul>
            <li><a href="#">Marca 1</a></li>
            <li><a href="#">Marca 2</a></li>
            <li><a href="#">Marca 3</a></li>
            <li><a href="#">Marca 4</a></li>
            <!-- Adicione mais links para filtrar por marca, se necessário -->
        </ul>
    </div>
</body>
</html>
